import { createSlice } from "@reduxjs/toolkit";

const initailState = {
    show: true
}

export const signupSlice = createSlice({
    name: 'signup',
    initailState,
    reducers: {

    }
})

export default 